import React from 'react';

const ReportList: React.FC = () => {
    return (
        <div>
            {/* Contenido original o placeholder para la lista de reportes */}
        </div>
    );
};

export default ReportList;